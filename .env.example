# MiniMax API Key
# Get it from: https://platform.minimax.io/user-center/basic-information/interface-key
MINIMAX_API_KEY=sk-your-api-key-here

# Public URL for callbacks (use ngrok or similar for local development)
# Example: https://your-subdomain.ngrok-free.app
PUBLIC_BASE_URL=https://your.ngrok.dev

# Server port
PORT=8000

# CORS Allowed Origins (comma-separated, optional)
# Leave empty to allow all origins (dev mode)
# Example: https://myapp.com,https://app.example.com
CORS_ORIGIN=

# Database (local Supabase)
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Supabase local (replace keys after running `npx supabase status`)
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=replace_with_local_anon_key
SUPABASE_SERVICE_ROLE_KEY=replace_with_local_service_role_key
